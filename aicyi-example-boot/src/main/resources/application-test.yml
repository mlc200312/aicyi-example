# 环境相关配置已迁移至 Nacos 配置中心
# dataId: aicyi-example-test.yml，group: DEFAULT_GROUP
# 配置内容见模块根目录 nacos/aicyi-example-test.yml，需先导入 Nacos 后再启动应用
spring:
  config:
    import:
      - nacos:aicyi-example-test.yml?group=DEFAULT_GROUP&refreshEnabled=true
