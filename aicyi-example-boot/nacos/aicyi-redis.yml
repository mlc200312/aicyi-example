spring:
  # redis配置
  redis:
    host: localhost
    port: 6379
    password: root
    database: 0
    timeout: 5000
    jedis:
      pool:
        max-active: 8
        max-wait: -1
        max-idle: 8
        min-idle: 0